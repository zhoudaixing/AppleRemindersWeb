import classNames from "classnames";
import "./index.css";

const SearchBar = () => {
  return (
    <div className={classNames("search-bar")}>
      <input />
    </div>
  );
};

export default SearchBar;
